<p id="notice"><%= notice %></p>

<h1>Listing Bloques</h1>

<table>
  <thead>
    <tr>
      <th>Tipo</th>
      <th>Descripcion</th>
      <th>Estado</th>
      <th>Localidad</th>
      <th>Ubicacion</th>
      <th>Custodio</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @bloques.each do |bloque| %>
      <tr>
        <td><%= bloque.tipos_bloque_id %></td>
        <td><%= bloque.descripcion %></td>
        <td><%= bloque.estados_bloque_id %></td>
        <td><%= bloque.localidad_id %></td>
        <td><%= bloque.ubicacion_id %></td>
        <td><%= bloque.user_id %></td>
        <td><%= link_to 'Show', bloque %></td>
        <td><%= link_to 'Edit', edit_bloque_path(bloque) %></td>
        <td><%= link_to 'Destroy', bloque, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Bloque', new_bloque_path %>
