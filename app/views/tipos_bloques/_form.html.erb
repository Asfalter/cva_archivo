<%= form_for(@tipos_bloque) do |f| %>
  <% if @tipos_bloque.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipos_bloque.errors.count, "error") %> prohibited this tipos_bloque from being saved:</h2>

      <ul>
      <% @tipos_bloque.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
