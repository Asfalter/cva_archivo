<%= form_for(@tipos_documento) do |f| %>
  <% if @tipos_documento.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipos_documento.errors.count, "error") %> prohibited this tipos_documento from being saved:</h2>

      <ul>
      <% @tipos_documento.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :descripcion %><br>
    <%= f.text_field :descripcion %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
